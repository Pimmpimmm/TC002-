name: portability

on:
  push:
  pull_request:

jobs:
  fresh-clone-macos:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: bash ci/verify-portability.sh
